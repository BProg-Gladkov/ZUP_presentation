
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ОбработкаОбъект = РеквизитФормыВЗначение("Объект");
	
	Для каждого Макет Из ОбработкаОбъект.Метаданные().Макеты Цикл
		Если НЕ СтрНачинаетсяС(Макет.Имя, "ШаблоныКода_") Тогда
			Продолжить;
		КонецЕсли;
		
		НоваяСтрока = ШаблоныКода.Добавить();
		НоваяСтрока.Имя = Макет.Синоним;
		НоваяСтрока.Код = ОбработкаОбъект.ПолучитьМакет(Макет.Имя).ПолучитьТекст();
	КонецЦикла; 
	
КонецПроцедуры
